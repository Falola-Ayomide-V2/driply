import PagePlaceholder from "@/components/ui/PagePlaceholder";

export default function CalendarPage() {
  return (
    <PagePlaceholder
      title="Calendar"
      subtitle="Plan what to wear, when."
      emptyTitle="No planned outfits"
      emptyDesc="Once built, your calendar entries will appear here."
    />
  );
}
